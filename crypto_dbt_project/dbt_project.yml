name: 'crypto_dbt_project'
version: '1.0'
config-version: 2

profile: 'crypto_dbt_project'

model-paths: ["models"]
target-path: "target"
clean-targets: ["target", "dbt_modules"]

models:
  crypto_dbt_project:
  +materialized: view
